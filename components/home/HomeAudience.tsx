/** Audience information remains visible to visitors and signed-in users. */
export default function HomeAudience({ children }: { children: React.ReactNode }) {
 return <div className="hero-choose">{children}</div>;
}
